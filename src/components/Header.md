A Header which accepts a props value title as the heading to be displayed

```js
import { GlobalProvider } from "../context/GlobalState";
import "../App.css";
<GlobalProvider>
  <Header title="Title" />
</GlobalProvider>;
```
